{
  "name": "ems-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && rm -rf dist && mv client/dist ./dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4"
  }
}
